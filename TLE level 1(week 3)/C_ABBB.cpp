#include <bits/stdc++.h>
using namespace std;
#define endl

#define int long long
const int MOD = 1e9 + 7;
const int INF = LLONG_MAX >> 1;
signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int tc;
    cin >> tc;
    while (tc--)
    {
        string s;
        cin>>s;
        string a = "AB";
        string b = "BB";
        size_t ind1 = s.find(a);
        size_t ind2 = s.find(b);
        if((ind1 != std::string::npos) || (ind1 != std::string::npos))
           
    }
}